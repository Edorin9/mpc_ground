name: mpc_ground
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  flutter_hooks: ^0.18.6
  freezed_annotation: ^2.2.0
  hydrated_bloc: ^9.1.2
  image_picker: ^0.8.7+1
  intl: ^0.18.1
  json_annotation: ^4.7.0
  mpc_ground_repository:
    path: packages/mpc_ground_repository
  path_provider: ^2.0.15

dev_dependencies:
  bloc_test: ^9.1.3
  build_runner: ^2.3.0
  flutter_test:
    sdk: flutter
  freezed: ^2.2.0
  json_serializable: ^6.5.4
  mocktail: ^0.3.0
  very_good_analysis: ^3.0.2

flutter:
  uses-material-design: true

assets:
  - assets/
